<!DOCTYPE html>
<html>
<head>
  <title>Rainbow Match</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
  <header class="header">
    <div class="header-bar custom-row"> <!-- クラス名を変更 -->
      <h1 class="custom-grid-6">
  <a href="/">
    <%= image_tag("Rainbow Match.png", alt: "Rainbow Match", class: "your-custom-class") %>
  </a>
</h1>
      <div class="user-nav custom-grid-6"> <!-- クラス名を変更 -->
      <% if user_signed_in? %>
        <li><%= link_to "マイページ", "/users/#{current_user.id}" %></li>
        <li><%= link_to '通知', user_notifications_path(current_user.id), class: "user-nickname" %></li>
        <li><%= link_to 'ログアウト',  destroy_user_session_path, data: { turbo_method: :delete }, class: "logout" %></li> 
      <% else %>
        <li><%= link_to 'ログイン', new_user_session_path, class: "login" %></li>
        <li><%= link_to '新規登録', new_user_registration_path, class: "sign-up" %></li>
      </div>
      <% end %>
      </div>
    </div>
  </header>
  <%= yield %>
  <footer>
    <p>
      Copyright Rainbow Match 2024.
    </p>
  </footer>
</body>
</html>

<div class="content_post" style="background-image: url(<%= url_for(user.image) %>);">
  <div class="more">
    <span><%= image_tag 'arrow_top.png' %></span>
    <ul class="more_list">
      <li>
        <%= link_to '詳細', user_path(user.id) %>
      </li>
    </ul>
  </div>
  <p><%= user.nickname %></p>
  <span class="name">
      <span>年齢</span> <%= ((Time.zone.now - user.birth_date.to_time) / 1.year.seconds).floor %>歳
   </a>
  </span>
</div>